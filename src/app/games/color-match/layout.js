export const metadata = {
    title: "Color Match Game — Test Your Focus",
    description:
        "Test your focus and reaction speed with PUZEXA's Color Match game. Identify the actual text color while ignoring the word.",
    alternates: {
        canonical: "/games/color-match",
    },
};

export default function Layout({ children }) {
    return children;
}