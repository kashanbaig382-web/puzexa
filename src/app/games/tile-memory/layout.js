export const metadata = {
  title: "Tile Memory Game — Remember the Pattern",
  description:
    "Play Tile Memory on PUZEXA. Memorize highlighted tiles, recreate the pattern and progress through increasingly difficult levels.",
};

export default function Layout({ children }) {
  return children;
}