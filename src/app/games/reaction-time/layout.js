export const metadata = {
  title: "Reaction Time Test — Test Your Reflexes",
  description:
    "Test your reaction speed with PUZEXA's free Reaction Time game. Wait for the signal, click as fast as possible and beat your best score.",
};

export default function Layout({ children }) {
  return children;
}